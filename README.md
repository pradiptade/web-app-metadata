This module implements two RESTful API endpoints.

/metadata/postMetadata:
Assumptions:
- the user passes the values (as YAML) one at a time.

/metadata/getDataByKey: 
- The key may or may not exist in the schema